mixin projectTile(name)

	- var project = contents.projects[name]['index.md']

	.project
		a(href=project.url)
			.container
				img(src='/projects/'+name+'/'+project.metadata.thumb)
			.info
				h2
					= project.metadata.title
				p
					= project.metadata.intro
